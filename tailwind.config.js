const colors = require("tailwindcss/colors");

module.exports = {
  darkMode: "class",
  purge: [],
  theme: {
    extend: {
      colors: {
        primary: colors.violet,
        gray: colors.coolGray,
      },
    },
  },
  variants: {},
  plugins: [],
};
